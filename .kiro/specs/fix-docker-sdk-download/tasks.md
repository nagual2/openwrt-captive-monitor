# Implementation Plan

- [x] 1. Улучшить скрипт загрузки SDK



  - Обновить `docker/sdk/download-sdk.sh` с улучшенной логикой определения суффикса MUSL
  - Добавить детальную диагностику ошибок
  - Добавить функцию для вывода списка доступных файлов при ошибке 404
  - Улучшить retry логику с экспоненциальной задержкой
  - Добавить поддержку продолжения прерванной загрузки
  - _Требования: 1.1, 1.2, 1.5, 2.1, 2.2, 2.3, 3.1, 3.2, 3.3_

- [ ]* 1.1 Написать property тест для формирования URL
  - **Property 1: Правильное формирование URL для всех архитектур**
  - **Валидирует: Требования 1.1, 1.2**
  - Тест должен проверить, что для каждой из 8 архитектур формируется корректный URL
  - Тест должен проверить, что URL существует на сервере (HTTP 200)
  - Минимум 100 итераций

- [ ]* 1.2 Написать property тест для верификации контрольной суммы
  - **Property 2: Успешная верификация контрольной суммы**
  - **Валидирует: Требования 1.3**
  - Тест должен проверить, что верификация проходит для корректных файлов
  - Тест должен проверить, что верификация падает для поврежденных файлов
  - Минимум 100 итераций

- [ ]* 1.3 Написать property тест для retry логики
  - **Property 3: Retry при сетевых ошибках**
  - **Валидирует: Требования 3.1, 3.3**
  - Тест должен проверить, что скрипт повторяет попытки при временных ошибках
  - Тест должен проверить экспоненциальную задержку между попытками
  - Минимум 100 итераций

- [ ]* 1.4 Написать property тест для продолжения загрузки
  - **Property 4: Продолжение прерванной загрузки**
  - **Валидирует: Требования 3.2**
  - Тест должен проверить, что curl использует опцию `-C -`
  - Тест должен проверить, что прерванная загрузка продолжается с места обрыва
  - Минимум 100 итераций

- [ ]* 1.5 Написать property тест для диагностики ошибок
  - **Property 5: Детальная диагностика ошибок**
  - **Валидирует: Требования 2.1, 2.2, 2.3, 2.4**
  - Тест должен проверить вывод полного URL при ошибке
  - Тест должен проверить вывод списка файлов при 404
  - Тест должен проверить вывод контрольных сумм при ошибке верификации
  - Минимум 100 итераций

- [ ]* 1.6 Написать property тест для распаковки SDK
  - **Property 6: Распаковка SDK в правильную директорию**
  - **Валидирует: Требования 1.4**
  - Тест должен проверить, что SDK распаковывается в `/opt/openwrt-sdk`
  - Тест должен проверить, что корневая директория из архива удаляется
  - Минимум 100 итераций

- [ ] 2. Создать unit тесты для скрипта
  - Создать `tests/docker/test_download_sdk.bats`
  - Написать тесты для функции `determine_musl_suffix()`
  - Написать тесты для функции `construct_sdk_url()`
  - Написать тесты для обработки ошибок
  - Настроить запуск тестов в CI
  - _Требования: 1.1, 1.2, 2.1, 2.2, 2.3, 2.4_

- [ ] 3. Checkpoint - Убедиться что все тесты проходят
  - Убедиться что все тесты проходят, спросить пользователя если возникают вопросы

- [ ] 4. Протестировать сборку Docker образов
  - Создать feature branch для тестирования
  - Запустить workflow `build-sdk-images.yml` вручную с force_rebuild=true
  - Проверить успешную сборку для всех 8 архитектур
  - Проверить логи сборки на наличие ошибок
  - Проверить размеры образов (должны быть < 2GB)
  - _Требования: 1.1, 1.2, 1.3, 1.4, 1.5_

- [ ] 5. Валидировать содержимое образов
  - Запустить скрипт `scripts/validate-docker-image-contents.sh` для каждого образа
  - Проверить наличие `/opt/openwrt-sdk` в образах
  - Проверить наличие необходимых инструментов (make, gcc, git)
  - Проверить работоспособность SDK (попробовать собрать простой пакет)
  - _Требования: 1.4_

- [ ] 6. Обновить документацию
  - Обновить комментарии в `docker/sdk/download-sdk.sh`
  - Добавить примеры использования в `docs/docker-sdk-images.md`
  - Документировать известные ограничения
  - Добавить troubleshooting секцию для ошибок загрузки
  - _Требования: 2.1, 2.2, 2.3, 2.4_

- [ ] 7. Final Checkpoint - Убедиться что все тесты проходят
  - Убедиться что все тесты проходят, спросить пользователя если возникают вопросы
